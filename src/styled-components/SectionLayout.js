import styled from "@emotion/styled";

const SectionLayout = styled.div`
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  align-items: center;
  gap: clamp(4rem, 10.666666vw, 10rem);
`;

export default SectionLayout;