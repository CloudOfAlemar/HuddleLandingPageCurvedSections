import styled from "@emotion/styled";

const BtnGetStarted = styled.button`
  font-size: clamp(1.2rem, 3.2vw, 2rem);
  line-height: 1.5em;
  padding: 0.916666em 3.333333em;
  border: none;
  border-radius: 2em;
  background-color: var(--pink);
  color: #FFFFFF;
`;

export default BtnGetStarted;